
export const sanitizerConfig = {
    // Allow custom elements such as 'ion-icon'
    ADD_TAGS: ["ion-icon"],
    // Allow specific attributes on the custom elements
    ADD_ATTR: ["name", "src", "size", "color", "aria-label", "id"],
};